services:
  - type: web
    name: jharkhand-tourism-chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GOOGLE_API_KEY
        sync: false   # Set via Render Dashboard
